# Aria MPS credentials for the extraction pipeline's `mps` stage.
#
# Copy this file to `aria_credentials.yaml` (git-ignored) and fill it in, or set
# ARIA_USERNAME / ARIA_PASSWORD env vars, or point ARIA_CREDENTIALS_FILE at a
# file elsewhere. If none are provided you'll be prompted interactively.
#
# NEVER commit a filled-in aria_credentials.yaml.
username: your-aria-account
password: your-aria-password
